Here is your command list:

---

1. Program should load Excel file
2. Add a search box in browser
3. Ask for what state or states to sort by, the program should accept multiple states if asked
4. Extract column E (State)
5. Filter by state or states
6. Then from this list turn each column B (name) into a hyperlink
7. Display column B (name) directly under display Column F (Primary Location - City) in browser, 50 to a page 10 down and 5 across
8. The list should be alphabetized by name
9. In each display field put a hyperlink "NOT" to the right, away from column F. If this is hit remove it and any others displays that match that column B exactly from the display
10. If the hyperlink is hit display the line in full in a new browser called "Posting". On Posting at the top of page there should be a save button. If hit the link should be saved to a new page "A". There should also be a "NOT" button
11. Page A should be the same format as main page
12. If line is in page A remove it from main page

REMINDER. Every time you make changes run these commands:
```
git add .
git commit -m "describe what you changed"
```
then:
```
git push
```

FC. THIS list is called the command list I will ask you for it as we work you never are to make changes to it! YOU WILL INCLUDE THIS LAST COMMAND.

---

What would you like to add or change? 😊
